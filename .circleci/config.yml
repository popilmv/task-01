version: 2.1
jobs:
  test:
    docker:
      - image: circleci/golang:1.15.8
    steps:
      - checkout
      - run: gofmt -e main.go
      - run:
          name: Slack notifications
          command: >
            curl -X POST -H 'Content-type: application/json' --data '{"text":"Failed pipeline, marina1327/task-01"}' https://hooks.slack.com/services/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
          when: on_fail  
  push:
    machine:
      image: ubuntu-2204:2022.04.2
    steps:
      - checkout
      - run:
          name: Login
          command: docker login -u $DOCKERHUB_USERNAME -p $DOCKERHUB_TOKEN
          # command: docker login -u $DOCKERHUB_USERNAME -p $DOCKERHUB_PASSWORD
          # command: echo "$DOCKER_PASSWORD" | docker login --username $DOCKER_USERNAME --password-stdin
      - run:
          name: Build
          command: docker build -t $DOCKERHUB_USERNAME/task-01:latest .
      - run:
          name: Push
          command: docker push $DOCKERHUB_USERNAME/task-01:latest
      - run:
          name: Slack notifications
          command: >
            curl -X POST -H 'Content-type: application/json' --data '{"text":"Failed pipeline, marina1327/task-01"}' https://hooks.slack.com/services/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
          when: on_fail 

workflows:
  circle-workflow:
    jobs:
      - test:
          filters:
            branches:
              only:
                - main
                - dev
            tags:
              only:
                - \d{1,2}\.\d{1,2}\.\d{1,2}
      - push:
          requires:
            - test
          filters:
            branches:
              only:
                - main
                - dev
            tags:
              only:
                - \d{1,2}\.\d{1,2}\.\d{1,2}